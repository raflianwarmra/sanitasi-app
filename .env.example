# Google Apps Script Web App URL for writing log entries back to Google Sheets
# Deploy a Apps Script as web app and paste the URL here
# If not set, logs are stored locally only (not persisted to sheet)
VITE_SCRIPT_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
